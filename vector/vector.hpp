#pragma once

namespace vec {
	template <typename T>
	class vector {
	public:
		~vector ();
		push_back (T input);

	private:
	};

	template <typename T>
	vector::push_back (T input) {
		int typeSize = sizeof (T);
	}

	template <typename T>
	vector::~vector () {
		
	}

}